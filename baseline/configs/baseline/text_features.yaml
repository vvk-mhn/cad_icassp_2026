name: text_features
system: text_features

# 1. Whisper ASR config
whisper_version: base.en
contractions_file: ${hydra:runtime.cwd}/recipes/cad_icassp_2026/baseline/contractions.csv

# 2. New model names from your research
semantic_model: all-MiniLM-L6-v2
nli_model: cross-encoder/nli-roberta-base

# 3. Standard batching
batch: 1
n_batches: 1
reference: processed